import { createFileRoute } from '@tanstack/react-router'
import { useEffect,useState } from 'react'
export const Route=createFileRoute('/owners')({component:OwnersPage})
export function OwnersPage(){const [owners,setOwners]=useState<any[]>([]);const [q,setQ]=useState('');const [error,setError]=useState<string|null>(null);async function load(){const r=await fetch(`/api/owners?q=${encodeURIComponent(q)}`);const b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(b.error?.message||'Unable to load owners');setOwners(b.owners||[])}useEffect(()=>{load().catch(e=>setError(e.message))},[]);return <section className="space-y-6"><div><h2 className="text-3xl font-semibold">Owners</h2><p className="mt-1 text-sm text-slate-600">Owner records linked to properties in this organization.</p></div><div className="flex gap-3"><input className="rounded-lg border bg-white px-3 py-2" placeholder="Search owner, phone, email" value={q} onChange={e=>setQ(e.target.value)}/><button className="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white" onClick={()=>load().catch(e=>setError(e.message))}>Search</button></div>{error?<div className="rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>:<div className="overflow-x-auto rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="border-b bg-slate-50"><tr>{['Owner','Phone','Email','Properties','Leads'].map(h=><th key={h} className="px-4 py-3 font-semibold">{h}</th>)}</tr></thead><tbody>{owners.map(o=><tr key={o.id} className="border-b last:border-0"><td className="px-4 py-3 font-medium">{o.name}</td><td className="px-4 py-3">{o.phone?<span title="Source value has not been independently verified">Unverified: {o.phone}</span>:'Not available'}</td><td className="px-4 py-3">{o.email?<span title="Source value has not been independently verified">Unverified: {o.email}</span>:'Not available'}</td><td className="px-4 py-3">{o.property_count}</td><td className="px-4 py-3">{o.lead_count}</td></tr>)}{!owners.length&&<tr><td colSpan={5} className="px-4 py-12 text-center text-slate-500">No owner records found.</td></tr>}</tbody></table></div>}</section>}
