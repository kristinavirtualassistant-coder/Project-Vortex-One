import React,{useState} from 'react';
import {createRoot} from 'react-dom/client';
import './styles.css';
function App(){const [user,setUser]=useState(localStorage.getItem('vortexUser'));const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');async function login(e){e.preventDefault();const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)return setError(d.error||'Login failed');localStorage.setItem('vortexToken',d.token);localStorage.setItem('vortexUser',JSON.stringify(d.user));setUser(JSON.stringify(d.user));}if(!user)return <div className="auth"><div className="card"><b>VORTEX ONE</b><h1>Sign in</h1><form onSubmit={login}><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button>Sign in</button></form><p>{error}</p></div></div>;const u=JSON.parse(user);return <div className="app"><aside><b>VORTEX ONE</b><nav><a className="active">Dashboard</a><a>Dialer</a><a>Leads</a><a>Campaigns</a><a>Templates</a><a>Reports</a><a>Settings</a></nav><button onClick={()=>{localStorage.clear();setUser(null)}}>Sign out</button></aside><main><h1>Dashboard</h1><p>Welcome, {u.name}</p><div className="grid"><div><span>Leads</span><strong>0</strong></div><div><span>Calls Today</span><strong>0</strong></div><div><span>Connected</span><strong>0</strong></div><div><span>Appointments</span><strong>0</strong></div></div><section><h2>Workspace ready</h2><p>Connect your database and telephony provider to begin live operations.</p></section></main></div>}
createRoot(document.getElementById('root')).render(<App/>);
